name: provider-flyio-native
runtime: yaml
plugins:
  providers:
    - name: flyio
      path: ../../bin

resources:
  myRandomResource:
    type: flyio:Random
    properties:
      length: 24

outputs:
  output:
    value: ${myRandomResource.result}
